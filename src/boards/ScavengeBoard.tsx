import { ReactElement } from "react";

import '../styles/board.css';

const images = [
    '/cocktailswords.png', 
    '/baubles.png', 
    '/straw.png', 
    '/crumbs.png', 
    '/rags.png', 
    '/flowers.png'
];

export const ScavengeBoard = (): ReactElement => {
    return (
        <div>
        </div>
    );
};